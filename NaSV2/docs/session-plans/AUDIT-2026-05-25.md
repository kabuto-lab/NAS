# AUDIT — AX•CMS / NaSV2 · 2026-05-25

> Deep state-of-the-repo audit, generated by AVTONOM month-bootstrap.
> Drives `ROADMAP-2026-05.md` and the 20 daily prompts that follow.
> Read top-to-bottom once; subsequent daily AVTONOMs reference only the
> sections they need.

- **MONTH_START:** 2026-05-25 (Mon)
- **MONTH_END:**   2026-06-19 (Fri) — last of 20 working days
- **Working days:** 20 (W1 May 25–29 · W2 Jun 1–5 · W3 Jun 8–12 · W4 Jun 15–19)
- **Pre-flight P0:** V1 cargo check green (cached, 3.2 s); V2/V3/V4 last
  ran 30 min ago in prior AVTONOM session — also green. No re-run needed.

---

## A1 · Stack inventory (per-crate)

LOC = non-blank non-comment lines under `src/`. Unit = `#[test]` /
`#[tokio::test]` count. Ignored = `#[ignore]` count (under whole crate).
Int = integration-test files in `tests/`. Bench = `benches/` present.
TODO = TODO/FIXME/unimplemented!/todo! occurrences.

| Crate | LOC | Unit | Ignored | Int | Bench | TODO | Status | Notes |
|---|---:|---:|---:|---:|:---:|---:|---|---|
| crates/common | 1 | 0 | 0 | 0 | – | 0 | **stub** | needs `TenantId`, `RequestId`, `AppError`, `Page<T>` per ENTITY §15, §20 |
| crates/domain | 1 | 0 | 0 | 0 | – | 0 | **stub** | needs `Site`, `Post`, `User`, `Role`, `Capability`, `Block` per ENTITY §21 |
| crates/application | 38 | 0 | 0 | 0 | – | 0 | **partial** | Only `ports::Queue` trait + types. Missing: PostRepository, UserRepository, MediaRepository, use cases |
| crates/infrastructure | 89 | 1 | 4 | 1 | – | 0 | **partial** | Only `queue::PgmqQueue` + 2 #[ignore] integration tests. Missing: persistence (repos), storage, email, search, cache |
| crates/extension-api | 1 | 0 | 0 | 0 | – | 0 | **stub** | needs semver contract for extension authors (ENTITY §14 capability surface) |
| crates/theme-api | 1 | 0 | 0 | 0 | – | 0 | **stub** | needs theme installer + image-variant manifest contract |
| crates/tenant | 1 | 0 | 0 | 0 | – | 0 | **stub** | needs `TenantContext` + tenant middleware (ENTITY §15) |
| crates/runtime | 268 | 3 | 0 | 0 | – | 0 | **functional** | `observability` + `supervisor` (3/3 unit). Missing: per-category Prometheus counters, Pyroscope hook |
| crates/presentation | 1 | 0 | 0 | 0 | – | 0 | **stub** | empty lib; no router/api/middleware/ssr/admin modules — blocks capability-coverage gate |
| crates/pool-validator | 70 | 2 | 6 | 1 | **Y** | 0 | **functional** | `ensure_transaction_mode` + bench + 4 integration tests (#[ignore]) |
| crates/image-pipeline | 30 | 0 | 0 | 0 | – | 1 | **stub** | needs libvips workers (ENTITY §3.7) + pgmq job consumer |
| crates/search-engine | 27 | 0 | 0 | 0 | – | 0 | **stub** | needs tantivy index (ENTITY §3.10 read path) |
| crates/edge-adapter | 27 | 0 | 0 | 0 | – | 1 | **stub** | needs CF Workers / Fastly contracts (ENTITY §3.10) |
| apps/server | 193 | 0 | 0 | 0 | – | 0 | **functional** | boot + 3 pools + pool-mode gate + 3 health endpoints + Http supervisor wired |
| apps/cli | 5 | 0 | 0 | 0 | – | 0 | **stub** | needs `db migrate`, `db seed-dev`, etc. per `CLAUDE.md` quick-start |
| xtask | 367 | 0 | 0 | 0 | – | 0 | **partial** | 3 real (pool-mode-check, bench-runner, architecture-check); 8 stubs |

**Counts:** 16 members · 9 stubs · 4 partial · 3 functional · 0 production.

**ENTITY §1–§10 violations spotted:**
- §8.1 (benchmark first) — only 1 bench exists (`pool_mode_from_str`).
  All §7 perf targets (10–20 ms p95 cached) currently have no measurement
  surface at all.
- §9.10 (overengineered microservices) — *not* violated; the empty crates
  are scaffolds, not running microservices. OK.
- §3.6 (sanitize on write) — no `ammonia` call anywhere in the tree.
  Will become a violation the moment first user-content path lands.

---

## A2 · Migration inventory

| File | Purpose | Direction | Rollback doc |
|---|---|---|---|
| `0001_pgmq_bootstrap.sql` | `CREATE EXTENSION pgmq` + 3 queues | expand-only (additive) | **missing** (no `docs/rollback/ROLLBACK-0001-pgmq.md`) |

ENTITY §17 requires "every migration must have a corresponding
`ROLLBACK-NNN-<slug>.md`" — currently violated for 0001. The existing
`docs/rollback/ROLLBACK-pool-validator.md` is for the validator crate,
not a migration. **Open recommendation R-mig-1.**

No applied 0002+ migrations exist on disk despite earlier commit
`7143bcf` mentioning "migration 0002 users/roles/capabilities" — those
were created in a prior branch and never re-instated in the current tree.

---

## A3 · Planning trail audit (RFC → ADR → PLAN → VAL)

| Slug | RFC | ADR | PLAN | VAL | Implemented? | Notes |
|---|:---:|:---:|:---:|:---:|:---:|---|
| cms-pages-publish | – | – | – | – | partial code in early commits | **orphan-code** — see `git log d149811..HEAD` |
| auth-foundation | – | – | – | – | code present | **orphan-code** — RFC/ADR existed in `e6b168e` but files not in tree |
| allocator-mimalloc | – | **ADR-002** | – | – | ✅ wired in `apps/server` | needs RFC backfill |
| pool-isolation | – | **ADR-003** | **PLAN-003** | **VAL-003** | ✅ pool-validator + ops/pgbouncer | needs RFC backfill |
| image-pipeline-libvips | – | **ADR-004** | **PLAN-001** | – | scaffold only | code = stub crate |
| queue-split-pgmq-nats | – | **ADR-005** | **PLAN-004** | – | pgmq adapter ✅; NATS – | partial |
| cache-multilevel | – | **ADR-006** | – | – | – | scaffold only |
| edge-architecture | **RFC-002** | – | – | – | – | RFC orphan — no ADR follow-up |
| pool-mode-contract | – | – | – | **VAL-002** | ✅ runtime gate | VAL without RFC/ADR/PLAN |
| task-supervisor | – | – | – | **VAL-004** | ✅ runtime supervisor | VAL without RFC/ADR/PLAN |
| search-engine-tantivy | – | – | **PLAN-002** | – | – | PLAN without ADR/VAL |

**Orphans summary:**
- 1 RFC without ADR (RFC-002)
- 2 ADRs without RFC (ADR-002, ADR-003)
- 2 VALs without RFC/ADR (VAL-002, VAL-004)
- 1 PLAN without RFC/ADR (PLAN-002)
- ≥ 2 features in code without any planning doc (cms-pages, auth)

**`cargo xtask check-planning-refs` would fail catastrophically** on any
commit message scanned today, because it expects RFC/ADR/PLAN/VAL refs in
commit messages and the trail is incomplete. The gate is a stub, so this
hasn't bitten yet — but the moment it's implemented (P6 of this roadmap),
CI will be red until backfill happens.

---

## A4 · xtask gate audit

From `xtask/src/main.rs` `Cmd` enum:

| Command | Status | ENTITY ref | Block? |
|---|---|---|---|
| `pool-mode-check` | ✅ real | §3.4.1 | – |
| `bench-runner` | ✅ real | §6.3 | – |
| `architecture-check` | ✅ real (this morning) | §2.6, §18.1 | – |
| `magic-check` | ❌ stub | §18.2 (forbid `lazy_static!`, raw `tokio::spawn`, oversized macros) | **HIGH** — currently CI dispatches the stub which prints "implementation pending" and returns 0. False security. |
| `capability-coverage` | ❌ stub | §14, §18.3 | MED — blocks once first handler lands |
| `check-planning-refs` | ❌ stub | §13, §18.4 | HIGH — see A3 above |
| `extension-audit` | ❌ stub | §9.5, §14 | LOW — no extensions exist yet |
| `pgo-build` | ❌ stub | §6.2 | LOW — production-only |
| `bolt-optimize` | ❌ stub | §6.2 | LOW — production-only |
| `alloc-budget` | ❌ stub | §10 (allocation analysis) | MED — needs dhat baseline first |
| `query-budget` | ❌ stub | §10 (EXPLAIN baseline) | MED — needs first repo query first |

**8 stubs out of 11.** CI calls 4 of them (`architecture-check`,
`magic-check`, `capability-coverage`, `check-planning-refs`) and 3 of
those 4 are stubs returning 0 — silent gate failure.

---

## A5 · CI audit

Workflows at `../../.github/workflows/`:

- `nasv2-ci.yml` — fmt, clippy, test --lib, xtask hygiene gates,
  cargo deny. **Issue:** `xtask hygiene gates` step runs 4 commands; 3
  are stubs that silently return 0. Functional coverage = 25%.
- `nasv2-nightly-bench.yml` — `cargo xtask bench-runner` + opens issue on
  >5% regression. **Issue:** no `docs/perf/baseline.json` exists yet —
  the very first nightly run will either crash or auto-set baseline on
  noisy ubuntu-latest hardware.
- `ci.yml` — parent-repo CI; out of scope.

**Missing CI:**
- No integration test job (`--ignored` with Docker)
- No `cargo doc --no-deps` check
- No `cargo audit` (advisory DB lookup beyond cargo deny)
- No `cargo bench --workspace --no-run` (would catch bench-crate
  compile drift early)

---

## A6 · Observability audit

`crates/runtime/src/observability.rs` (122 LOC) provides:

✅ JSON `fmt` layer + `EnvFilter` (`LOG_LEVEL` env, default `info`)
✅ OTLP exporter (conditional on `OTLP_ENDPOINT`) with batch span processor
✅ Prometheus HTTP listener (conditional on `PROMETHEUS_METRICS_PORT`)
✅ `ObservabilityGuard` flushes on drop
✅ TaskSupervisor spans (added in P1 today) — every spawned task carries
   `category`, `name`, `task_id` UUID

**Missing per ENTITY §4:**
- Pyroscope continuous-profiling hook (`pyroscope` + `pyroscope_pprofrs`
  are in workspace deps but not wired)
- `dhat` heap-snapshot mode under a `feature = "dhat-heap"` flag (for
  non-prod allocation profiling — §4 explicit)
- per-category Prometheus counters
  (`tasks_spawned_total{category="queue"}` etc.) — needs first non-HTTP
  supervisor consumer
- Sentry/GlitchTip — explicitly deferred by §4 until real users (OK)
- Cardinality budget enforcement (no current limiter on label cardinality
  — risk when first repo metric lands)

---

## A7 · Security / threat surface

`docs/security/SEC-001-sanitization-pipeline.md` exists but the
**sanitization pipeline is not wired** anywhere — `ammonia` is a workspace
dep but no call site exists. ENTITY §3.6 says "sanitize on write" — the
moment first user-content write path lands, this becomes a P0 security gap.

**Capability hash in cache key (ENTITY §3.9.1):** no cache is wired yet
(moka/foyer not consumed) so not yet a violation. Becomes a gap when
first cached read lands.

**RLS coverage (ENTITY §3.3 + §15):** `TenantContext` doesn't exist yet
(`crates/tenant` is a stub); no `set_config('app.tenant_id', ...)` call
in the tree. Will become P0 when first tenant-scoped repo method lands.

**JWT verification:** committed in earlier branches (`7143bcf
JwtVerifier`), not in current tree.

**Threat-model documents missing:**
- SEC-002 capability surface (which capabilities, who grants them)
- SEC-003 multi-tenant isolation invariants
- SEC-004 secret-handling (secrecy::SecretString placement)

---

## A8 · Performance baseline

`docs/perf/` contains only `PERF-001-targets-galactic.md` (targets — no
measurements). `docs/perf/baseline.json` does NOT exist. The nightly
bench workflow (`nasv2-nightly-bench.yml`) will fail or auto-init on its
first run.

The single existing bench (`pool_mode_from_str`) has no baseline either —
just `cargo bench --workspace --no-run` proves it compiles.

**No measurement against the §7 hard targets exists in the repo:**
| §7 target | Status |
|---|---|
| Cached read p95 10–20 ms | not measured |
| Cached read throughput ≥ 10 K req/s/core | not measured |
| Cold start ≤ 300 ms | not measured |
| Memory baseline ≤ 50 MB RSS | not measured |
| Memory per warm tenant ≤ 1 MB | not measured (no tenant infra yet) |

---

## A9 · Open recommendations from last 3 SESSION_LOGs

Cross-referenced against current code state (closed = code present, open
= still needed).

Latest `SESSION_LOG.md` (AVTONOM 2026-05-25 10:01, this morning):

| # | Recommendation | State |
|---:|---|---|
| 1 | Run `bench-runner` for baseline | **open** — no `baseline.json` |
| 2 | Run pool-validator `--ignored` tests | **open** — needs operator Docker run |
| 3 | Try architecture-check on PR | open — needs PR-flow |
| 4 | Promote presentation→infrastructure WARN→ERROR | **open** — blocked on Phase-B DI refactor |
| 5 | Wire architecture-check into CI | **partial** — `nasv2-ci.yml` lists it but does not gate on stubs |
| 6 | Build & push pgbouncer Dockerfile | open — operator action |
| 7 | Implement xtask magic-check | **open** — natural P1 follow-up (this month) |
| 8 | Backfill RFC-003 + ADR-003 for TaskSupervisor | **open** — see A3 orphans |

Older session-logs absorbed into the 2026-05-25 report (5 recs already
closed in P4). Net open from history: **7 of 8**.

---

## A10 · Risk register (top 10, by likelihood × impact)

| # | Risk | Likelihood | Impact | Source |
|---:|---|---|---|---|
| 1 | CI silent-passes the 3 stub gates (`magic-check`, `capability-coverage`, `check-planning-refs`) — false confidence | **High** | **High** | A4 |
| 2 | First nightly bench runs without baseline → noisy regression alerts or auto-baseline on shared runner | High | Med | A5, A8 |
| 3 | RFC-002 (edge architecture) sits orphan; future edge work has no constitutional anchor | Med | Med | A3 |
| 4 | `crates/presentation` empty stub — capability-coverage gate cannot be implemented until first handler lands; planning trail blocked | High | Med | A1 |
| 5 | No `ammonia` sanitization call in tree; first user-content path will introduce silent XSS | Med | **High** | A7 |
| 6 | No `TenantContext` — first repo query will violate ENTITY §15 unless tenant infra arrives first | High | **High** | A1, A7 |
| 7 | Workspace `Cargo.toml` accumulates unused deps (regex/serde_json just removed from domain — others?) | Low | Low | A1 |
| 8 | LF↔CRLF noise on every commit (Windows host) → diff churn, false review burden | High | Low | git status output |
| 9 | rustfmt unstable-keys spam (`empty_item_single_line`, etc.) ignored every fmt run | High | Low | observed in V2 logs |
| 10 | Domain crate has zero types — every downstream layer is blocked on its first concrete aggregate | Cert. | **High** | A1 (domain LOC=1) |

---

## A11 · Unfinished sessions / blocked items

From 2026-05-25 `SESSION_LOG.md` "Skipped / Blocked" section:

| Item | Reason | Suggested follow-up |
|---|---|---|
| P5 capability-coverage real impl | `crates/presentation/src` has no handlers | **must wait** until first handler — see G4 in ROADMAP |
| pgmq integration test execution | needs `PGMQ_IMAGE` env + Docker | operator action; CI nightly candidate (out of bootstrap scope) |
| `cargo bench --workspace` full run | noise on dev machine | operator action; G5 captures CI nightly wiring |
| `cargo deny`, `magic-check`, `check-planning-refs` real impls | out of P0–P7 scope | G5 covers `magic-check` + `check-planning-refs`; deny already in CI as action |

---

## A12 · Capacity model

20 working days × 8 h productive = **160 h** ceiling.

Budget split (per session-plan defaults):

| Bucket | % | Hours | Notes |
|---|---:|---:|---|
| P0 verification (per-day) | 30% | 48 h | ~2.5 h/day baseline; goes up on red days |
| Feature work | 50% | 80 h | The 4-5 ROADMAP goals |
| Refactor / debt | 15% | 24 h | Stub cleanup, dep trim, fmt drift |
| Docs (RFC/ADR/PLAN/VAL/RETRO) | 5% | 8 h | Planning trail backfill is high-leverage |

Slack: 1 day per week buffered (W1 D5 has lighter scope; W4 has retro day).

**Per-day output ceiling assumption:** 1 substantial commit + 1 docs
commit per AVTONOM session. Past two AVTONOMs landed 5–7 commits — but
each was 30–45 min. A 4 h day allows ~3 phase-commits comfortably.

---

## Ideas for next month (NOT in this roadmap)

Captured here per bootstrap default "if AUDIT surfaces an out-of-scope
idea, record but do NOT roadmap":

- **WASM extension sandbox** (ENTITY §9.5) — needs wasmtime/wasmer
  evaluation; ADR-required; defer to month 2.
- **Foyer hybrid mem+SSD L1 cache** vs moka — both in workspace deps,
  benchmark contest; defer.
- **`compio`/`monoio` TPC microservice for image worker** (ENTITY §3.13)
  — needs ADR + bench evidence Tokio is the bottleneck; defer.
- **Theme installer + bundled `themes/minimal`** — requires `theme-api`
  contract first; defer until §3 frontend gets its first Leptos island.

---

## Working day calendar (W1–W4)

| Week | Mon | Tue | Wed | Thu | Fri |
|---|---|---|---|---|---|
| W1 | 2026-05-25 | 2026-05-26 | 2026-05-27 | 2026-05-28 | 2026-05-29 |
| W2 | 2026-06-01 | 2026-06-02 | 2026-06-03 | 2026-06-04 | 2026-06-05 |
| W3 | 2026-06-08 | 2026-06-09 | 2026-06-10 | 2026-06-11 | 2026-06-12 |
| W4 | 2026-06-15 | 2026-06-16 | 2026-06-17 | 2026-06-18 | 2026-06-19 |

W4 D5 (2026-06-19) is reserved for `RETRO-2026-06.md` + next-month
bootstrap generation per session-plan template.
