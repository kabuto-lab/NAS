# WEEK-01 · May 25–29, 2026

> Theme: **Gate hardening + domain bootstrap kickoff**
> Goals covered: **G1** (xtask gates) + start of **G2** (common/domain)
> Generated by AVTONOM month-bootstrap from `ROADMAP-2026-05.md`.

## Daily slots

| Date | Day | Daily prompt file | Scope summary | Est h |
|---|---|---|---|---:|
| 2026-05-25 | Mon | `daily/2026-05-25.md` | G1.P1 · xtask `magic-check` real impl (regex scan for raw `tokio::spawn` + `lazy_static!` + oversized macros) + 1st violation test | 4 |
| 2026-05-26 | Tue | `daily/2026-05-26.md` | G1.P2 · xtask `check-planning-refs` real impl (parses commit msgs for RFC/ADR/PLAN/VAL refs) + opt-out for trivial commits | 4 |
| 2026-05-27 | Wed | `daily/2026-05-27.md` | G1.P3 · CI verify + magic-check on existing tree (will likely flag prior code — fix or whitelist) | 3 |
| 2026-05-28 | Thu | `daily/2026-05-28.md` | G2 kick-off · `nas2-common` skeleton: `TenantId`, `SiteId`, `UserId`, `RequestId` newtypes + serde + garde + unit tests | 4 |
| 2026-05-29 | Fri | `daily/2026-05-29.md` | G2 continue · `nas2-common` finish: `AppError` (thiserror), `Page<T>` (pagination wrapper), final tests; spill into domain skeleton | 4 |

## Dependencies satisfied entering this week

- Workspace compiles green (verified P0 morning of 2026-05-25 in prior AVTONOM)
- `nas2-runtime::TaskSupervisor` exists — magic-check needs to ban
  raw `tokio::spawn` everywhere **except** `crates/runtime/src/supervisor.rs`
- `xtask::commands::architecture_check` already real — pattern to follow

## Definition of done (W1)

- [ ] `cargo xtask magic-check` exits non-zero on a synthetic
  `tokio::spawn()` planted in any non-runtime crate; exits 0 on clean tree
- [ ] `cargo xtask check-planning-refs --commit HEAD` exits 0 on a
  commit msg containing `RFC-NNN` / `ADR-NNN` / `PLAN-NNN` / `VAL-NNN`;
  exits 1 otherwise (with trivial-commit allow-list: typo/dep-bump/comment)
- [ ] `nas2-common` exports ≥ 5 public types with serde derive + tests
- [ ] Workspace stays green for all P0 gates (check / fmt / clippy / lib tests)
- [ ] 5 daily commits + 0 push
- [ ] `SESSION_LOG.md` overwritten 5 times, last one carries any
  CARRY-OVER for W2 D1

## Carry-over policy

If day N runs over, day N+1's prompt begins with a
`## CARRY-OVER from <date>` section that the operator (or the next
AVTONOM) MUST resolve before starting that day's P1.

Pattern:

```
## CARRY-OVER from 2026-05-26
- magic-check: regex for `lazy_static!` accidentally matches the doc-comment
  regex inside `crates/runtime/src/supervisor.rs` — need word-boundary fix
- TODO: re-run cargo xtask magic-check against tree after fix
```

If a carry-over still exists at the end of W1 D5, it cascades into
W2 D1's prompt.

## Risks specific to W1

| Risk | Mitigation |
|---|---|
| `check-planning-refs` real impl flags every commit in current history → CI red | implement with `--commit HEAD` default and a `since` arg so only NEW commits are checked; backfill comes in W4 G5 |
| `magic-check` regex false-positives on doc-comments | use ripgrep-style word-boundary patterns + skip lines inside `//!` / `///` / `/* */` |
| `nas2-common::AppError` design ripples into every later crate | keep the variant set MINIMAL (≤ 5 variants); add later as needed |
