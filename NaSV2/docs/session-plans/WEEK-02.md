# WEEK-02 · Jun 1–5, 2026

> Theme: **Domain core completion + application ports kickoff**
> Goals covered: finish **G2** (domain), start **G3** (application)
> Generated by AVTONOM month-bootstrap from `ROADMAP-2026-05.md`.

## Daily slots

| Date | Day | Daily prompt file | Scope summary | Est h |
|---|---|---|---|---:|
| 2026-06-01 | Mon | `daily/2026-06-01.md` | G2 · `nas2-domain::site` (`Site` aggregate + `SiteSlug` value object + creation invariants) | 4 |
| 2026-06-02 | Tue | `daily/2026-06-02.md` | G2 · `nas2-domain::post` (`Post`, `PostStatus`, `PostSlug`, `CustomPostType` placeholder) | 4 |
| 2026-06-03 | Wed | `daily/2026-06-03.md` | G2 · `nas2-domain::user` + `::role` + `::capability` (typed `Capability` enum + `Role` with capability set + `User` aggregate) | 4 |
| 2026-06-04 | Thu | `daily/2026-06-04.md` | G2 finish · `nas2-domain::block` enum (typed content blocks: Heading, Paragraph, Image, CodeBlock — extensible) + final architecture-check pass | 4 |
| 2026-06-05 | Fri | `daily/2026-06-05.md` | G3 kickoff · `nas2-application::ports::post_repository` + `::user_repository` + `::site_repository` (`async-trait` signatures + `mockall` doubles) | 4 |

## Dependencies satisfied entering this week

- `nas2-common` complete from W1 D4–D5 (TenantId, RequestId, AppError, Page<T>)
- `cargo xtask magic-check` + `check-planning-refs` real and gating CI

## Definition of done (W2)

- [ ] `nas2-domain` exports `Site`, `Post`, `User`, `Role`, `Capability`,
  `Block` with serde + thiserror + unit tests (≥ 12 unit tests total)
- [ ] `Capability` is an exhaustive typed enum (NOT a `&'static str`),
  with `as_str()` for serialization, matching `cms.page.read` /
  `cms.page.publish` / `media.upload` shape from §14
- [ ] `cargo xtask architecture-check` still ok — domain has only
  `{serde, uuid, chrono, garde, thiserror, nas2-common}` deps
- [ ] `nas2-application::ports::PostRepository` trait dyn-safe; mockall
  generates a `MockPostRepository` for use-case unit tests
- [ ] 5 daily commits + 0 push

## Carry-over policy

Same pattern as W1. If W2 D5 still has carry-over, it cascades into
W3 D1's `daily/2026-06-08.md`.

## Risks specific to W2

| Risk | Mitigation |
|---|---|
| `Block` enum grows uncontrollably → §8.2 premature abstraction | start with 4 variants (Heading, Paragraph, Image, CodeBlock); add only when use case demands |
| `Capability` enum becomes 50+ variants on day 1 | start with ≤ 6 (read, publish, draft, upload, manage_users, manage_site); document growth path |
| `Post` aggregate accidentally pulls async via `chrono::DateTime<Utc>` → architecture-check breaks | `chrono` is on the domain allow-list — no risk; double-check via `cargo xtask architecture-check` after each commit |
| `mockall` macro hygiene on dyn-safe traits | use `#[automock]` on trait, NOT on impl; verify with explicit `_assert_object_safe(_: &dyn Trait) {}` test |
