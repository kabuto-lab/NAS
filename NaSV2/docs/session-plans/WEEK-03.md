# WEEK-03 · Jun 8–12, 2026

> Theme: **Application finish + tenant middleware + first presentation handler**
> Goals covered: finish **G3**, start **G4**
> Generated by AVTONOM month-bootstrap from `ROADMAP-2026-05.md`.

## Daily slots

| Date | Day | Daily prompt file | Scope summary | Est h |
|---|---|---|---|---:|
| 2026-06-08 | Mon | `daily/2026-06-08.md` | G3 · `nas2-application::queries::get_published_page_by_slug` use case (composes PostRepository + capability check) + unit tests with MockPostRepository | 4 |
| 2026-06-09 | Tue | `daily/2026-06-09.md` | G3 · `nas2-tenant::TenantContext` (newtype-ish wrapper) + tenant middleware (axum `from_fn_with_state`) that resolves Host header / signed cookie → `TenantId` + populates `request.extensions()` | 4 |
| 2026-06-10 | Wed | `daily/2026-06-10.md` | G3 · capability-hash helper (`CapabilitySet::hash() -> u64`) for future cache-key composition (§3.9.1); MediaRepository port stub (for §3.7 future); G3 close-out unit tests | 4 |
| 2026-06-11 | Thu | `daily/2026-06-11.md` | G4 start · `nas2-presentation` router skeleton: `Router<AppState>`, `tenant_resolver` middleware mounted, `/health/*` parity with apps/server, OpenAPI scaffold via utoipa | 4 |
| 2026-06-12 | Fri | `daily/2026-06-12.md` | G4 · `GET /api/v1/pages/:slug` handler — extracts `TenantContext` + `CapabilitySet`, requires `cms.page.read`, calls `GetPublishedPageBySlug`, maps to JSON; in-memory `PostRepository` test double + first axum `oneshot()` integration test | 4 |

## Dependencies satisfied entering this week

- `nas2-domain` complete (Site / Post / User / Role / Capability / Block)
- `nas2-application::ports` includes PostRepository, UserRepository, SiteRepository
- `MockPostRepository` available for use-case tests
- xtask gates green

## Definition of done (W3)

- [ ] `GetPublishedPageBySlug` use case has ≥ 4 unit tests covering: ok,
  not-found, missing capability, tenant mismatch
- [ ] `nas2-tenant::TenantContext` middleware tested with axum's
  `IntoResponse` mock; missing-header path returns 400 with structured
  error
- [ ] `CapabilitySet::hash()` is deterministic across runs (test
  asserts; needed for §3.9.1 cache key composition later)
- [ ] `nas2-presentation::router::build_router()` exists and returns a
  `Router<AppState>` that survives `cargo check`
- [ ] One axum `oneshot()` integration test passes against
  `GET /api/v1/pages/:slug`
- [ ] `cargo xtask architecture-check` still ok (presentation→infrastructure
  remains documented WARN; not introduced new violations)
- [ ] 5 daily commits + 0 push

## Carry-over policy

Same. If W3 D5 still has carry-over related to G4, W4 D1's prompt
starts with `## CARRY-OVER from 2026-06-12` and the handler completion
becomes the first half of W4 D1.

## Risks specific to W3

| Risk | Mitigation |
|---|---|
| `TenantContext` resolution path entangles with JWT validation prematurely | resolve from `Host` header only this week; signed-cookie path deferred to month 2 |
| Capability-hash collision design needs cryptographic discussion | use `xxh3_64` (or `std::hash::DefaultHasher` if `xxhash-rust` not in workspace) — not cryptographic; document non-security purpose in code |
| First handler accidentally goes through `nas2-infrastructure` directly | use the test-double `PostRepository` in `AppState`; real sqlx repo deferred to month 2 to keep G4 scope small |
| utoipa OpenAPI macro pulls heavy compile time | keep schemas minimal this week; defer doc generation polish |
