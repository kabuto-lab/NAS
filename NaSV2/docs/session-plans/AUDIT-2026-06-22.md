# AUDIT — AX•CMS / NaSV2 · 2026-06-22 (Month 2 entry — forward-projected)

> **Forward-projected audit.** Authored on 2026-05-25 alongside the
> 12-month master roadmap. Documents the state we EXPECT to enter
> Month 2 with, assuming M1 ROADMAP-2026-05 ships as planned.
>
> **On 2026-06-22 the actual M2 bootstrap re-runs this audit against
> reality** — any divergence reshapes M2 goals. Do not treat this
> document as committed truth; it's a starting point for the new
> bootstrap.

- **MONTH_START:** 2026-06-22 (Mon)
- **MONTH_END:**   2026-07-17 (Fri)
- **Working days:** 20 (W5..W8)

---

## A1 · Stack inventory — projected entering state

| Crate | Status at M2 start | Notes |
|---|---|---|
| common | **functional** | TenantId/SiteId/UserId/RoleId/PostId/MediaId/RequestId + Page<T> + AppError + sanitize |
| domain | **functional** | Site/Post/Block (4 variants)/Capability/CapabilitySet/Role/User/Email/Media (placeholder) |
| application | **partial** | ports::{Post,User,Site,Media}Repository + queries::GetPublishedPageBySlug + Queue |
| tenant | **partial** | TenantContext + InMemoryTenantResolver + resolve_tenant middleware |
| runtime | **functional** | TaskSupervisor + observability |
| presentation | **partial** | AppState + router + caps stub + 1 handler + IntoResponse |
| infrastructure | **partial** | PgmqQueue only — NO PostgreSQL repos yet |
| pool-validator | **functional** | + bench |
| image-pipeline, search-engine, edge-adapter, extension-api, theme-api | **stubs** | scheduled for later months |
| apps/server | **functional** | 3 pools, pool-mode gate, http supervisor wired |
| apps/cli | **stub** | will gain `db migrate` in M2 W5 D5 |
| xtask | **partial** | 5/11 real (pool-mode + bench + architecture + magic + planning-refs); capability-coverage real |

Workspace test count entering M2: **~60+ unit tests, ~5 integration**

---

## A2 · Migrations inventory (entering)

- `0001_pgmq_bootstrap.sql` (from M1 W2 D2) — pgmq + 3 queues
- *(nothing else)*

**M2 will add ≥ 5 migrations** for tenant-scoped tables + RLS policies.

---

## A3 · Planning trail (entering)

After M1's backfill (W4 D3):
- RFC-001 cms-pages-publish
- RFC-002 edge-architecture (orphan — deferred)
- RFC-003 task-supervisor
- RFC-004 pgmq-queue
- ADR-001 cms-pages-architecture
- ADR-002 allocator-mimalloc
- ADR-003 pool-isolation
- ADR-004 image-pipeline-libvips
- ADR-005 queue-split-pgmq-nats
- ADR-006 cache-multilevel
- PLAN-001 image-pipeline / PLAN-002 search-engine (orphan; deferred)
- PLAN-003 pgbouncer-split-pool / PLAN-004 pgmq-bootstrap
- VAL-001 cms-pages-tests / VAL-002 pool-mode / VAL-003 pool-isolation / VAL-004 task-supervisor
- ROLLBACK-pool-validator

**M2 will add ≥ 6 docs**:
- RFC-005 csrf-strategy
- RFC-006 jwt-auth-foundation
- ADR-009 revisions-autosaves
- ADR-010 rls-policy-shape (the multi-statement convention)
- VAL-005 rls-isolation-property (proptest)
- ROLLBACK-0002 / ROLLBACK-0003 / ROLLBACK-0004 / ROLLBACK-0005 / ROLLBACK-0006

---

## A4 · xtask gates entering M2

| Command | Status |
|---|---|
| architecture-check | real |
| magic-check | real |
| check-planning-refs | real (not yet wired in CI) |
| capability-coverage | real (1 handler scanned today) |
| pool-mode-check | real |
| bench-runner | real |
| extension-audit | stub |
| pgo-build / bolt-optimize | stub (production-only — M12) |
| alloc-budget / query-budget | stub |

**M2 picks up:** ① wire `check-planning-refs` into CI now that
backfill is complete (W5 D1 carries this); ② consider lifting
`alloc-budget` from stub for the first repo work (W6 D5).

---

## A5 · CI workflows entering

- `.github/workflows/nasv2-ci.yml` — fmt + clippy + lib tests +
  xtask hygiene + cargo deny (M2 W5 wires `check-planning-refs
  --since main~20` into the xtask-gates job)
- `.github/workflows/nasv2-nightly-bench.yml` — bench-runner +
  issue-on-regression
- **Missing:** integration-test job with Docker; M2 W6 D5 proposes
  + writes the patch to SESSION_LOG (operator merges)

---

## A6 · Observability entering

`crates/runtime/src/observability.rs` ships JSON tracing + OTLP +
Prometheus. Per-category metrics counters (`tasks_spawned_total{
category}`) NOT yet wired — wait until first non-HTTP supervisor
exists (M3 image worker).

Pyroscope, dhat — workspace deps present, no consumers yet; defer
to M3 W4 when image worker has measurable CPU cost.

---

## A7 · Security entering

- `nas2_common::sanitize::clean_html` (ammonia) shipped W4 D4
- Capability surface enumerated (SEC-002)
- Multi-tenant isolation invariants documented (SEC-003)
- **NOT yet implemented:**
  - RLS policy SQL applied in migrations (M2 G1)
  - JWT verification (M2 G3)
  - CSRF nonce (M2 G5)
  - Application passwords (deferred to M3)

---

## A8 · Performance baseline entering

`docs/perf/baseline.json` exists (W4 D4) — only `pool_mode_from_str`
captured. No real workload benches yet. M2 adds `repo_find_by_slug`
bench in W6 D5 as the first DB-touching microbench.

---

## A9 · Open recommendations (after M1 RETRO)

(Predicted — actual list comes from `RETRO-2026-06.md` when 06-19 lands.)

1. Wire `check-planning-refs --since` into CI → M2 W5 D1
2. Add integration-test job to CI (Docker container per run) →
   M2 W6 D5
3. Capture perf baseline on a quiet machine → operator action;
   not blocking M2
4. ADR for `xxhash-rust` cross-process cache key stability →
   defer to M10 when L2 cache lands

---

## A10 · Risk register for M2

| # | Risk | Likelihood | Impact | Mitigation |
|---:|---|---|---|---|
| 1 | RLS policy SQL has a subtle bypass via missing `app.tenant_id` set_config | Med | **High** | proptest in VAL-005 — randomised tenant insert/read across 1000 rounds |
| 2 | sqlx `query!` macro forces DB at compile time | High | Med | use `sqlx::query_as` + runtime SQL; `.sqlx/` snapshot offline mode (workspace already gitignores .sqlx) — pick runtime path for now |
| 3 | JWT key rotation story unclear | Med | Med | M2 G3 ships a single-key path; rotation = ADR for M3 |
| 4 | testcontainers + Windows host = flaky CI | Med | Med | run integration tests locally with explicit `cargo test -- --ignored`; CI integration job in W6 D5 uses ubuntu-latest |
| 5 | `with_tenant` helper signature design churns | Low | Med | settle in W6 D1 with ADR-010 |

---

## A11 · Unfinished items entering

From M1 deferred to M2 (or later):
- `xtask check-planning-refs` CI wiring
- production pgbouncer Dockerfile push (operator)
- bench baseline re-capture on quiet machine (operator)

---

## A12 · Capacity model

20 working days × 8 h = 160 h. Splits:
- 30% P0 verification = 48 h
- 50% feature work = 80 h (G1 RLS 25 h, G2 repo 20 h, G3 JWT 15 h,
  G4 CRUD 20 h)
- 15% refactor / debt = 24 h (mocks-to-real swaps, schema iteration)
- 5% docs (RFC-005/006 + ADR-009/010 + VAL-005) = 8 h

Slack: 1 day per week + W8 D5 RETRO = 5 days of buffer.

---

## Working day calendar (M2)

| Week | Mon | Tue | Wed | Thu | Fri |
|---|---|---|---|---|---|
| W5 | 2026-06-22 | 2026-06-23 | 2026-06-24 | 2026-06-25 | 2026-06-26 |
| W6 | 2026-06-29 | 2026-06-30 | 2026-07-01 | 2026-07-02 | 2026-07-03 |
| W7 | 2026-07-06 | 2026-07-07 | 2026-07-08 | 2026-07-09 | 2026-07-10 |
| W8 | 2026-07-13 | 2026-07-14 | 2026-07-15 | 2026-07-16 | 2026-07-17 |
