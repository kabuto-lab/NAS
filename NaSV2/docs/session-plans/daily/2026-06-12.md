AVTONOM: AX•CMS · W3 D5 · G4.P2 — GET /api/v1/pages/:slug handler.
Дата: 2026-06-12 (пт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · First handler: GET /api/v1/pages/:slug
  H1. crates/presentation/src/api/pages.rs (NEW):
        use axum::{extract::{Path, State, Extension}, Json};
        use nas2_application::queries::GetPublishedPageBySlug;
        use nas2_application::ports::PostRepository;

        pub async fn get_page_by_slug<R: PostRepository + 'static>(
            State(state): State<AppState>,
            Extension(tctx): Extension<TenantContext>,
            // Capability extraction — на сегодня stub: всегда даём
            // CmsPageRead. JWT-derived caps появятся в month 2.
            Path(slug): Path<String>,
        ) -> Result<Json<Post>, AppError> {
            let slug = PostSlug::try_new(&slug)?;
            let caps = CapabilitySet::from_iter([Capability::CmsPageRead]);
            // ⚠️ caps construction stub — see TODO comment + PLAN-G4
            let uc = GetPublishedPageBySlug::new(state.post_repo.clone());
            let post = uc.execute(tctx.tenant_id, tctx.site_id, &slug, &caps).await?;
            Ok(Json(post))
        }
      ВАЖНО: добавить comment-маркер `// caps.require(cms.page.read)`
      выше функции — это нужно чтобы capability-coverage regex
      (которая придёт в W4 D2) его нашла.
  H2. crates/presentation/src/api/mod.rs: pub mod pages;
  H3. crates/presentation/src/router.rs: добавить route
        .route("/api/v1/pages/:slug", axum::routing::get(api::pages::get_page_by_slug::<…>))
      Здесь генерик закрывается через Arc<dyn PostRepository> в AppState — see
      AppState refactor: store as `Arc<dyn PostRepository>` (уже было в W3 D4).
      handler signature становится `(State<AppState>, Extension<TenantContext>, Path<String>)`
      без generic; внутри тащит `state.post_repo.clone()` (Arc clone).
  H4. Integration test (crates/presentation/tests/get_page_by_slug.rs):
        use axum::{body::Body, http::Request};
        use tower::ServiceExt;
        ... setup AppState с MockPostRepository (mockall) + один Site/Tenant ...
        #[tokio::test] async fn returns_200_for_published_page() {
            let app = build_router(state);
            let req = Request::builder()
                .uri("/api/v1/pages/hello-world")
                .header("host", "example.com")
                .body(Body::empty()).unwrap();
            let resp = app.oneshot(req).await.unwrap();
            assert_eq!(resp.status(), 200);
        }
      → файл-scope #[allow(clippy::expect_used, ::unwrap_used, ::panic)] как в
      других integration-тестах.

P2 · architecture-check + clippy
  A1, A2 — стандартно.

P3 · Final
  Коммит: `feat(ax/g4-w3d5,PLAN-G4): GET /api/v1/pages/:slug handler + first oneshot integration test`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Capability extraction — stub (всегда даём CmsPageRead).
    Реальная capability resolution из JWT — month 2 (нужен JwtVerifier).
  • Comment-marker `// caps.require(cms.page.read)` обязателен —
    capability-coverage gate (W4 D2) опирается на него.
  • Mock-based test only — реальный sqlx repo wiring отложено.
  • 401 / 404 / 400 status codes покрываются в W4 D1 (отдельный день).

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
