AVTONOM: AX•CMS · W4 D4 · G5.P3 — perf baseline + ammonia + SEC docs.
Дата: 2026-06-18 (чт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · First perf baseline
  B1. Запустить локально на тихой машине (если ноут под нагрузкой —
      SKIP P1 и записать в SESSION_LOG как «operator deferred»):
        cargo bench -p nas2-pool-validator --bench pool_mode_check -- --save-baseline initial
      Результаты криетриона:
        target/criterion/pool_mode_from_str/*/{base,new}/estimates.json
  B2. Извлечь медианы (mean.point_estimate) и записать в
      docs/perf/baseline.json (НОВЫЙ файл):
        {
          "version": 1,
          "captured_at": "2026-06-18",
          "captured_on": "developer-host (noisy)",
          "tolerance_pct": 5,
          "benches": {
            "pool_mode_from_str/transaction": <ns>,
            "pool_mode_from_str/session":     <ns>,
            "pool_mode_from_str/statement":   <ns>,
            "pool_mode_from_str/weird-future-mode": <ns>
          }
        }
      Документировать в самом файле что noise floor → каждое обновление
      должно делаться на той же машине / в той же CI среде.

P2 · Ammonia wiring — заменить sanitize placeholder
  S1. crates/common/Cargo.toml: добавить `ammonia = { workspace = true }`.
  S2. crates/common/src/sanitize.rs (overwrite):
        pub fn clean_html(input: &str) -> String {
            // ENTITY §3.6 — sanitize on write only. Allow-list:
            // tags: p, br, strong, em, u, h1..h6, ul, ol, li, blockquote,
            //       code, pre, a, img
            // attrs: a[href], img[src,alt]
            // url_schemes: http, https, mailto
            use ammonia::Builder;
            Builder::default()
                .add_allowed_classes("code", &["language-rust","language-js"])
                .url_schemes(hashset!{"http","https","mailto"})
                .clean(input)
                .to_string()
        }
        // deprecate placeholder
        #[deprecated(note = "use clean_html instead")]
        pub fn clean_html_placeholder(input: &str) -> String { input.to_owned() }
  S3. Unit tests (≥ 4):
        test_strips_script_tag
        test_keeps_allowed_tags
        test_filters_javascript_url_scheme
        test_keeps_mailto_link

P3 · Security docs
  D1. docs/security/SEC-002-capability-surface.md (NEW, ~100 LOC):
        Lists all Capability variants from nas2-domain::capability,
        кто issuer (admin role), кто consumer (handlers), как hash
        попадает в cache key (§3.9.1 reference).
  D2. docs/security/SEC-003-multi-tenant-isolation.md (NEW, ~120 LOC):
        TenantId resolution path, set_config('app.tenant_id', ...)
        contract, RLS policies (placeholder — real SQL приходит month 2),
        threat model: cross-tenant data leak via accidental SELECT.

P4 · architecture-check + clippy + lib tests
P5 · Final
  Один коммит: `feat(ax/g5-w4d4,SEC-002,SEC-003,VAL-002): ammonia
  sanitize-on-write + perf baseline + SEC-002/003 docs`.
  (Multiple refs in one msg — check-planning-refs treats first as primary.)

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • baseline.json формат — простой JSON, без version-2 escape; обновлять
    только когда оператор явно запустит на quiet machine + commit'нет.
  • ammonia allow-list — узкий (см. S2). Расширение через RFC.
  • Деprecate placeholder — оставляем для нескольких релизов чтоб
    случайные импорты не сломались.
  • SEC docs — НЕ полный threat model, MVP-уровень.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
