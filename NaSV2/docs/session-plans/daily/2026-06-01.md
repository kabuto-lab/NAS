AVTONOM: AX•CMS · W2 D1 · G2.P3 — nas2-domain::site.
Дата: 2026-06-01 (пн). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P0.5 · Micro-audit (first Monday after major scope shift)
  Прочитать SESSION_LOG за W1 D5 (2026-05-29). Если CARRY-OVER not
  empty — добавить его в P1 этого дня ПЕРЕД остальным scope.

P1 · `nas2-domain::site` aggregate
  S1. Создать crates/domain/src/site.rs:
        pub struct Site {
            pub id: SiteId,
            pub tenant_id: TenantId,
            pub slug: SiteSlug,
            pub display_name: String,
            pub created_at: chrono::DateTime<chrono::Utc>,
        }
        pub struct SiteSlug(String);  // value object с invariant
        impl SiteSlug {
            pub fn try_new(raw: &str) -> Result<Self, AppError>
            // invariants: 3-64 chars, [a-z0-9-], не начинается с дефиса,
            // не содержит подряд два дефиса
        }
  S2. crates/domain/src/lib.rs: pub mod site; pub use site::{Site, SiteSlug};
  S3. crates/domain/Cargo.toml: проверить что есть chrono + serde +
      nas2-common (уже было). Никаких новых workspace deps.
  S4. Unit tests (≥ 6):
        test_site_slug_valid_examples
        test_site_slug_rejects_too_short / too_long
        test_site_slug_rejects_uppercase
        test_site_slug_rejects_leading_hyphen
        test_site_slug_rejects_double_hyphen
        test_site_serde_roundtrip (Site full struct)

P2 · architecture-check
  A1. cargo run -p xtask -- architecture-check → expect ok (1 WARN).

P3 · Final
  F1. SESSION_LOG.md.
  F2. Коммит: `feat(ax/g2-w2d1,PLAN-G2): nas2-domain::site + SiteSlug value object`.
      Trailer AI-Assisted.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Slug regex: `^[a-z0-9](?:[a-z0-9]|-[a-z0-9])*$`, длина 3..=64.
    Реализовать через ручную validation (не regex crate — `regex` НЕ в
    domain allow-list).
  • `chrono` уже в domain allow-list — OK.
  • `SiteSlug::try_new` возвращает `AppError::Validation`.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
