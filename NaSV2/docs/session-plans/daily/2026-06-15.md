AVTONOM: AX•CMS · W4 D1 · G4.P3 — handler error mapping + 401/404/400 tests.
Дата: 2026-06-15 (пн). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P0.5 · Micro-audit (Monday)
  Прочитать SESSION_LOG за W3 D5 (2026-06-12). CARRY-OVER → впихнуть.

P1 · Capability extraction helper (`nas2-presentation::caps`)
  C1. crates/presentation/src/caps.rs:
        /// Extracts CapabilitySet from request. Today: stub returns
        /// fixed set { CmsPageRead }. Month 2: derived from JWT.
        ///
        /// Marker comment for `xtask capability-coverage`:
        ///   caps.require(cms.page.read)
        pub fn extract_caps_for_today() -> CapabilitySet {
            CapabilitySet::from_iter([Capability::CmsPageRead])
        }
  C2. crates/presentation/src/api/pages.rs: заменить inline CapabilitySet
      construction на caps::extract_caps_for_today() — точка для будущего
      JWT-wiring (month 2).

P2 · Negative-path tests (401 / 404 / 400)
  T1. crates/presentation/tests/get_page_by_slug.rs — добавить три
      test'а:
        returns_403_when_caps_missing — override caps stub via
          test-only constructor `extract_caps_for_test(CapabilitySet)`
          (добавить #[cfg(test)] export)
        returns_404_when_slug_not_found — MockPostRepository вернёт None
        returns_400_when_slug_invalid_chars — slug "BAD slug!" не пройдёт
          PostSlug::try_new
      Все через axum::ServiceExt::oneshot + tower body assertions.
      Всего на конец дня — 4 integration tests в файле.

P3 · architecture-check + clippy + lib tests
  Все три зелёные.

P4 · Final
  Коммит: `feat(ax/g4-w4d1,PLAN-G4): caps extraction helper + 403/404/400 integration tests for GET pages`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • caps::extract_caps_for_test — gated `#[cfg(test)]`. Production
    handlers НЕ имеют доступа.
  • 401 vs 403 — используем 403 (Forbidden) для missing-capability.
    401 зарезервируем для missing-auth (когда JWT layer появится).
  • Тест returns_403 проверяет именно 403 (а не 401) — это контрактный
    выбор.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
