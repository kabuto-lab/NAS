AVTONOM: AX•CMS · W4 D3 · G5.P2 — planning trail backfill (RFC/ADR/VAL).
Дата: 2026-06-17 (ср). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · Создать недостающие docs для оrphan trail (см. AUDIT A3)
  Каждый — single markdown file, ~80-150 строк, структура по аналогии с
  существующими VAL-004 / PLAN-004.

  R1. docs/rfc/RFC-001-cms-pages-publish.md
      «Why» — почему CMS должен публиковать страницы с typed Block enum
      вместо HTML blob (TLA L1+L4). Цель — backfill для cms-pages
      кода в early commits + W3-W4 handler.

  R2. docs/rfc/RFC-003-task-supervisor.md
      «Why» — почему raw tokio::spawn запрещён; failure-domain isolation
      по TaskCategory; bounded drain. Backfill для VAL-004.

  R3. docs/rfc/RFC-004-pgmq-queue.md
      «Why» — почему pgmq (а не NATS первым); transactional guarantees;
      worker_pool isolation. Backfill для PLAN-004.

  R4. docs/adr/ADR-001-cms-pages-architecture.md
      «How» — структура aggregate Post + Block enum; serde tagged
      representation; ammonia sanitize-on-write обязательство.

  R5. docs/validations/VAL-001-cms-pages-tests.md
      «How verified» — unit tests count в nas2-domain + integration
      test в presentation; criterion bench будущего endpoint.

  Каждый file должен содержать секцию `## References` с явным
  cross-link обратно к RFC/ADR/PLAN/VAL партнёрам — чтоб трейл стал
  graph-walkable.

P2 · Run `cargo xtask check-planning-refs --commit HEAD~10` (или с того
     момента когда trail стал bottom-up complete)
  Ожидание: pass для всех future commits начиная с сегодняшнего.
  Если есть исторические orphan commits — log в SESSION_LOG как
  «historical debt, accepted».

P3 · Final
  Коммит: `docs(ax/g5-w4d3,RFC-001,RFC-003,RFC-004,ADR-001,VAL-001):
  planning trail backfill — 5 docs`.
  Trailer AI-Assisted.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Docs — короткие, без воды. ~100 строк каждая. Структура: Context /
    Decision / Consequences / TLA-layers / References.
  • НЕ переписывать существующие commit messages — backfill — это NEW
    docs.
  • Если AUDIT A3 содержит ещё orphans — взять до 5 в этот день;
    остальное — в RETRO как «leftover».

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
