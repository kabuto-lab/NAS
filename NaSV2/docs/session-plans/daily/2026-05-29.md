AVTONOM: AX•CMS · W1 D5 · G2.P2 — nas2-common finish (AppError + final tests).
Дата: 2026-05-29 (пт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-common::error::AppError`
  E1. Создать crates/common/src/error.rs.
      #[derive(Debug, thiserror::Error)]
      pub enum AppError {
          #[error("not found: {0}")]
          NotFound(String),
          #[error("forbidden: {0}")]
          Forbidden(String),
          #[error("validation failed: {0}")]
          Validation(String),
          #[error("conflict: {0}")]
          Conflict(String),
          #[error("internal error")]
          Internal,
      }
      Дополнительно: `impl AppError { pub fn http_status(&self) -> u16 { ... } }`
      (404/403/400/409/500) — для презентации в W3.
  E2. Документация: KEEP MINIMAL. Каждый вариант обоснован одним
      предложением в doc-comment.
  E3. crates/common/Cargo.toml — добавить `thiserror = { workspace = true }`.
  E4. crates/common/src/lib.rs: `pub mod error; pub use error::AppError;`
  E5. Unit tests:
        test_app_error_display_includes_message
        test_app_error_http_status_mapping
        test_app_error_is_send_sync (compile-time)

P2 · `nas2-common::sanitize` placeholder
  S1. Создать crates/common/src/sanitize.rs со СТАБ-импл:
        /// Placeholder — real ammonia wiring lands in W4 D4.
        /// Returns input unchanged. DO NOT use for untrusted HTML yet.
        pub fn clean_html_placeholder(input: &str) -> String { input.to_owned() }
      + единственный тест:
        test_placeholder_is_identity
      Это резервирует имя modul'я; реальный ammonia wiring — W4 D4 (G5.P3).
      `#[deprecated(note = "...")]` НЕ ставить — ещё пуст, deprecate когда
      реальный clean_html появится.

P3 · architecture-check + clippy
  A1. cargo run -p xtask -- architecture-check → expect ok.
  A2. cargo clippy --workspace --all-targets -- -D warnings → expect ok.

P4 · Final
  F1. SESSION_LOG.md.
  F2. Коммит: `feat(ax/g2-d5,PLAN-G2): nas2-common AppError + sanitize placeholder`.
      Trailer AI-Assisted.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • AppError variants — 5 штук (NotFound, Forbidden, Validation, Conflict,
    Internal). Расширение — только когда есть конкретный use case.
  • sanitize.rs — ЯВНЫЙ placeholder (имя другое: clean_html_placeholder)
    чтоб случайные пользователи не подумали что HTML очищается.
  • Spine touch не нужен.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
