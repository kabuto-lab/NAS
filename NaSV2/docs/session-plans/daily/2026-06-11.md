AVTONOM: AX•CMS · W3 D4 · G4.P1 — nas2-presentation router skeleton.
Дата: 2026-06-11 (чт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-presentation::app_state` + router skeleton
  R1. crates/presentation/src/app_state.rs:
        pub struct AppState {
            pub tenant_resolver: TenantResolver,
            pub post_repo: std::sync::Arc<dyn PostRepository>,
            // больше repo по мере роста; пока — один
        }
        impl AppState {
            pub fn new(tr: TenantResolver, post_repo: Arc<dyn PostRepository>) -> Self
        }
  R2. crates/presentation/src/router.rs:
        pub fn build_router(state: AppState) -> axum::Router {
            axum::Router::new()
                .route("/health/live",  axum::routing::get(health_live))
                .route("/health/ready", axum::routing::get(health_ready))
                // /api/v1/pages/:slug — приходит завтра (W3 D5)
                .layer(axum::middleware::from_fn_with_state(
                    state.tenant_resolver.clone(),
                    nas2_tenant::middleware::resolve_tenant,
                ))
                .with_state(state)
        }
        async fn health_live() -> &'static str { "ok" }
        async fn health_ready() -> &'static str { "ready" }
  R3. crates/presentation/src/error.rs:
        // impl IntoResponse для AppError
        impl axum::response::IntoResponse for AppError {
            fn into_response(self) -> axum::response::Response {
                let status = match self.http_status() {
                    400 => StatusCode::BAD_REQUEST,
                    403 => StatusCode::FORBIDDEN,
                    404 => StatusCode::NOT_FOUND,
                    409 => StatusCode::CONFLICT,
                    _   => StatusCode::INTERNAL_SERVER_ERROR,
                };
                let body = serde_json::json!({"error": self.to_string()});
                (status, axum::Json(body)).into_response()
            }
        }
  R4. crates/presentation/src/lib.rs:
        pub mod app_state;
        pub mod router;
        pub mod error;
  R5. crates/presentation/Cargo.toml: убедиться что nas2-tenant,
      nas2-application, nas2-common, axum, serde_json есть в deps.
      (Если nas2-tenant нет — добавить.)
  R6. Unit tests (≥ 2):
        test_router_health_live_returns_ok — через tower::ServiceExt oneshot
        test_app_error_into_response_maps_404 — direct call

P2 · architecture-check
  A1. expect ok (1 WARN). Если addon nas2-tenant создаёт новый warn —
      обновить логику architecture-check ИЛИ задокументировать как «дополнительный
      Phase-B refactor target».

P3 · Final
  Коммит: `feat(ax/g4-w3d4,PLAN-G4): nas2-presentation — router skeleton + AppState + AppError IntoResponse`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • `Arc<dyn PostRepository>` — выбран dyn (а не generic) для
    AppState простоты. Конкретная performance оценка — month 2.
  • IntoResponse возвращает плоский JSON `{"error": "msg"}` — без
    stack trace, без error code — этого достаточно для MVP.
  • Health endpoints — простые строки. /health/pool НЕ дублируется
    из apps/server — это server-level concern.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
