AVTONOM: AX•CMS · W2 D5 · G3.P1 — application::ports kickoff.
Дата: 2026-06-05 (пт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-application::ports::post_repository`
  P1. crates/application/src/ports/post_repository.rs:
        #[async_trait::async_trait]
        pub trait PostRepository: Send + Sync {
            async fn find_by_slug(
                &self,
                tenant: TenantId,
                site: SiteId,
                slug: &PostSlug,
            ) -> Result<Option<Post>, AppError>;

            async fn list_published(
                &self,
                tenant: TenantId,
                site: SiteId,
                page: u32,
                per_page: u32,
            ) -> Result<Page<Post>, AppError>;

            async fn insert(&self, post: &Post) -> Result<(), AppError>;

            async fn update(&self, post: &Post) -> Result<(), AppError>;
        }
      Default attrs: `#[cfg_attr(test, mockall::automock)]` для mock generation.

P2 · `nas2-application::ports::user_repository`
  U1. find_by_id, find_by_email, insert, update — те же signatures с
      Tenant. Без role expansion (роли — отдельный repo).
      `#[cfg_attr(test, mockall::automock)]`.

P3 · `nas2-application::ports::site_repository`
  S1. find_by_id, find_by_slug, list_all_for_tenant, insert.
      mockall как выше.

P4 · ports/mod.rs обновить — re-exports + проверка dyn-safety
  T1. crates/application/src/ports/mod.rs: pub mod post_repository / user_repository /
        site_repository. Re-export traits.
  T2. Тест на dyn-safety (compile-time, не runtime):
        #[cfg(test)] mod tests {
            fn _assert_post_repo_object_safe(_: &dyn PostRepository) {}
            // ... аналогично для остальных
        }
  T3. crates/application/Cargo.toml: добавить `mockall = { workspace = true }`
      в [dev-dependencies] (НЕ в [dependencies] — mockall не должен попадать
      в production builds).

P5 · architecture-check + clippy + lib tests
  Все три должны пройти. Особое внимание: nas2-application не должен
  обзавестись sqlx/tokio/axum deps. mockall — только dev.

P6 · Final
  Коммит: `feat(ax/g3-w2d5,PLAN-G3): nas2-application::ports — PostRepository + UserRepository + SiteRepository + mockall doubles`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Все методы — `async fn` через async_trait. После Rust 1.85 — можно
    native async fn in traits, но это требует рассмотрения; не сегодня.
  • mockall — dev-only.
  • `list_published` возвращает `Page<Post>` (для пагинации).
  • НЕТ MediaRepository сегодня — отложено на W3 D3.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
