AVTONOM: AX•CMS · W1 D3 · G1.P3 — CI gate verification + fixes.
Дата: 2026-05-27 (ср). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · Run всех xtask gates на сегодняшнем tree
  G1. `cargo run -p xtask -- architecture-check` → expect ok (1 WARN)
  G2. `cargo run -p xtask -- magic-check` → expect ok (0 нарушений)
  G3. `cargo run -p xtask -- capability-coverage` → stub (вернёт 0)
  G4. `cargo run -p xtask -- check-planning-refs --commit HEAD` → expect
      FAIL для прошлых коммитов; добавить --since <SHA-первого-нашего-этого-месяца>
      чтоб проверять только новые. Default-since в импл изменить НЕ нужно —
      операторский флаг достаточен.
  Все логи → docs/session-logs/avtonom-2026-05-27-xtask-*.log

P2 · Fix mass-violations если они есть в новом tree
  F1. Если magic-check нашёл что-то — починить non-spine. Если в spine —
      записать в SESSION_LOG как «debt», SKIP.
  F2. Если architecture-check внезапно красный (новые deps?) — починить
      non-spine. Скорее всего green остаётся.

P3 · CI workflow update
  W1. Прочитать ../.github/workflows/nasv2-ci.yml.
  W2. **НЕ ИЗМЕНЯТЬ его** — это per-deploy-config файл вне NaSV2/.
      Вместо этого: задокументировать в SESSION_LOG раздел «CI
      recommendation» с конкретным diff-suggestion для оператора:
      добавить `cargo xtask check-planning-refs --since main~10` в
      jobs.xtask-gates (defensive — последние 10 коммитов).
  W3. Update docs/session-plans/HOW-TO-RUN.md (если уже существует от
      Phase E bootstrap) с новым «CI verification» секцией;
      если ещё нет — в SESSION_LOG это запиши как «pending until
      Phase E lands today».

P4 · Final
  F1. SESSION_LOG.md.
  F2. Коммит (зависит от того что было fix'нуть):
      a) если ничего не fix'нули — НЕТ коммита для W2 (только session_log
         не коммитим, он перезаписывается в начале следующей сессии);
         в этом случае один коммит `chore(ax/g1-d3,PLAN-G1): xtask gate
         verification — clean tree`.
      b) если что-то fix'нули — `fix(ax/g1-d3,PLAN-G1): clear magic-check
         violations after G1.P1 wiring`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Spine touch не нужен сегодня.
  • CI workflow file (../.github/workflows/nasv2-ci.yml) — NOT
    modified автономно (за пределами NaSV2/).
  • check-planning-refs failure для исторических коммитов — НЕ ошибка,
    задокументировать.

═════════════════════════════════════════════════════════════════════
HARD STOPS
═════════════════════════════════════════════════════════════════════

(стандартный набор)

═════════════════════════════════════════════════════════════════════
ЗАПРЕЩЕНО АБСОЛЮТНО
═════════════════════════════════════════════════════════════════════

(стандарт)

═════════════════════════════════════════════════════════════════════
SESSION_LOG.md формат — стандарт
═════════════════════════════════════════════════════════════════════
