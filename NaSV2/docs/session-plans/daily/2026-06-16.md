AVTONOM: AX•CMS · W4 D2 · G5.P1 — xtask capability-coverage real impl.
Дата: 2026-06-16 (вт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · xtask `capability-coverage` real impl
  C1. Создать xtask/src/commands/capability_coverage.rs.
      Сканирует crates/presentation/src/**/*.rs (walkdir + regex).
      Для каждой pub-функции, имя которой соответствует одному из
      шаблонов:
        - заканчивается на `_handler`
        - находится внутри файла, лежащего под `api/` (по path)
        - имеет doc-comment маркер `/// HTTP:` или сигнатуру вида
          `async fn ... -> ... Result<...> ...` в контексте axum::routing
      Требуется: либо в теле функции есть либо в строках комментариев
      перед сигнатурой есть подстрока `caps.require(`. ALT: явный
      doc-attr `#[no_capability_required]` (стаб-attr, пока не существует
      реально — просто marker для регекса).
      Алгоритм пока — heuristic (regex), не AST. Документировать
      ограничение в исходнике.
      Exit 1 если найден violation; exit 0 на clean tree.
  C2. xtask/src/commands/mod.rs: pub mod capability_coverage;
  C3. xtask/src/main.rs: route Cmd::CapabilityCoverage → real impl
      (spine mini-edit АВТОРИЗОВАН).
  C4. Unit tests:
        test_detects_handler_without_caps_require
        test_passes_handler_with_caps_marker_comment
        test_passes_handler_with_no_capability_required_attr
        test_skips_non_handler_pub_fn
  C5. Запустить `cargo run -p xtask -- capability-coverage` против
      crates/presentation/ — ожидание: ok (handler from W3-W4 имеет
      marker comment).

P2 · architecture-check + clippy + lib tests
P3 · Final
  Коммит: `feat(ax/g5-w4d2,PLAN-G5): xtask capability-coverage real impl + 4 unit tests`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Regex-based heuristic — НЕ полный AST. Документировать в
    src как known limitation; AST-версия — отдельная задача (month 3+).
  • `#[no_capability_required]` attribute — placeholder, не реализован
    в коде. Просто marker — regex его ищет, ничего более.
  • Если capability-coverage внезапно обнаружит handler без marker
    (т.е. handler был добавлен между W3 D5 и сегодня и забыл marker) —
    починить (добавить marker), затем re-run.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
