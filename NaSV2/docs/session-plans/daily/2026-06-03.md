AVTONOM: AX•CMS · W2 D3 · G2.P5 — nas2-domain user/role/capability.
Дата: 2026-06-03 (ср). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-domain::capability` — typed enum
  C1. crates/domain/src/capability.rs:
        #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
        pub enum Capability {
            CmsPageRead,
            CmsPagePublish,
            CmsPageDraft,
            MediaUpload,
            ManageUsers,
            ManageSite,
        }
        impl Capability {
            pub const fn as_str(self) -> &'static str { match self { ... "cms.page.read" ... } }
            pub fn try_from_str(s: &str) -> Result<Self, AppError>
        }
        pub struct CapabilitySet(std::collections::BTreeSet<Capability>);
        impl CapabilitySet {
            pub fn new() -> Self
            pub fn from_iter(...) -> Self
            pub fn contains(&self, c: Capability) -> bool
            pub fn insert(&mut self, c: Capability)
            pub fn hash_xx(&self) -> u64  // placeholder hash — DefaultHasher
            //                               (xxh3 — отдельная задача в W3 D3)
        }
  C2. Unit tests (≥ 6):
        test_capability_as_str_roundtrip
        test_capability_try_from_str_unknown_returns_err
        test_capability_set_contains
        test_capability_set_hash_deterministic
        test_capability_set_serde

P2 · `nas2-domain::role` aggregate
  R1. crates/domain/src/role.rs:
        pub struct Role {
            pub id: RoleId,           // → nas2-common::ids
            pub tenant_id: TenantId,
            pub name: String,
            pub capabilities: CapabilitySet,
        }
        impl Role {
            pub fn has(&self, c: Capability) -> bool { self.capabilities.contains(c) }
        }
  R2. crates/common/src/ids.rs: добавить RoleId.
  R3. Unit tests (≥ 3): test_role_has_capability / test_role_serde /
        test_role_no_capabilities_means_no_access.

P3 · `nas2-domain::user` aggregate
  U1. crates/domain/src/user.rs:
        pub struct User {
            pub id: UserId,
            pub tenant_id: TenantId,
            pub email: Email,         // value object
            pub role_ids: Vec<RoleId>,
            pub created_at: chrono::DateTime<chrono::Utc>,
        }
        pub struct Email(String);
        impl Email {
            pub fn try_new(raw: &str) -> Result<Self, AppError>
            // invariant: contains @, len 6..=254, no whitespace
        }
  U2. Unit tests (≥ 5): valid email examples / no-at-rejected / too-long /
        too-short / user_serde_roundtrip.

P4 · architecture-check + clippy
P5 · Final
  Коммит: `feat(ax/g2-w2d3,PLAN-G2): nas2-domain User+Role+Capability+CapabilitySet`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Email validation: ручная (no regex crate — не в allow-list).
    Достаточно: содержит '@', не содержит whitespace, длина 6..=254.
    RFC 5322 — НЕ реализовывать, отложено.
  • CapabilitySet::hash_xx — пока DefaultHasher. xxh3 wiring через
    `xxhash-rust` — отдельная задача (но xxhash-rust НЕТ в workspace
    deps; добавлять НЕ нужно сегодня — DefaultHasher достаточно).
  • Capability enum — 6 variants. Расширение через PR.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
