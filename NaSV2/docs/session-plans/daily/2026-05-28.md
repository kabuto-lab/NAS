AVTONOM: AX•CMS · W1 D4 · G2.P1 — nas2-common skeleton (newtypes).
Дата: 2026-05-28 (чт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-common` — newtype IDs
  N1. Создать crates/common/src/ids.rs со следующими newtypes (все
      `Uuid` под капотом, `#[serde(transparent)]`, `Copy + Hash + Eq`):
        - `TenantId(Uuid)`
        - `SiteId(Uuid)`
        - `UserId(Uuid)`
        - `RequestId(Uuid)` — `pub fn new() -> Self { Self(Uuid::new_v7()) }`
          (v7 — time-ordered, лучше для индексов)
      Каждый: `impl Display { write!("{}", self.0.simple()) }`
      Каждый: метод `new_v4()` / `new_v7()` где уместно.
      `TenantId / SiteId / UserId` — `new_v4()` (random, не time-leaking).
  N2. Создать crates/common/src/page.rs:
        pub struct Page<T> { pub items: Vec<T>, pub total: u64,
                              pub page: u32, pub per_page: u32 }
        impl<T> Page<T> {
            pub fn new(items, total, page, per_page) -> Self
            pub fn empty() -> Self
            pub fn map<U>(self, f: impl FnMut(T) -> U) -> Page<U>
        }
        Derive: Debug, Clone, Serialize, Deserialize<T: Serialize/Deserialize>.
  N3. crates/common/src/lib.rs:
        pub mod ids;
        pub mod page;
        pub use ids::{TenantId, SiteId, UserId, RequestId};
        pub use page::Page;
  N4. crates/common/Cargo.toml — добавить из workspace deps:
        uuid (with v4 + v7 + serde features), serde, thiserror (для P2 завтра).
      НЕ добавлять: tokio, sqlx (нарушит §2.6 domain-adjacency).
  N5. Unit tests в каждом модуле:
        ids.rs: test_tenant_id_serde_roundtrip / test_request_id_is_time_ordered /
                 test_tenant_id_display_format
        page.rs: test_page_map_preserves_meta / test_page_empty / test_page_serde

P2 · Architecture-check confirm
  A1. cargo run -p xtask -- architecture-check → ожидание ok (1 WARN
      существующий). Если новый WARN/ERROR на nas2-common — починить
      (вероятнее всего ничего не упадёт, common уже в allow-list для
      domain).

P3 · Final
  F1. SESSION_LOG.md.
  F2. Коммит: `feat(ax/g2-d4,PLAN-G2): nas2-common newtypes — TenantId/SiteId/UserId/RequestId + Page<T>`
      Trailer AI-Assisted.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Spine touch не нужен.
  • RequestId использует v7 (time-ordered). Все остальные — v4 (privacy).
  • Page<T> — без cursor pagination (offset/limit подход) — keep MVP simple.
  • thiserror добавляется завтра (P2 D5 будет AppError).

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО АБСОЛЮТНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
