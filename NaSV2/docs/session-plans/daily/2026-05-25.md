AVTONOM: AX•CMS · W1 D1 · G1.P1 — xtask `magic-check` real impl.
Дата: 2026-05-25 (пн). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none — W1 D1)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate
  V1 cargo check --workspace --all-targets → docs/session-logs/avtonom-2026-05-25-cargo-check.log
  V2 cargo fmt --all (apply)
  V3 cargo clippy --workspace --all-targets -- -D warnings → лог
  V4 cargo test --workspace --lib --no-fail-fast → лог
  Лимит 5 итераций V1/V3. Если красное и нечинибельное → SKIP P1+, full
  report, HARD STOP.

P1 · xtask `magic-check` real impl
  M1. Создать xtask/src/commands/magic_check.rs.
      Использовать walkdir + regex (уже в xtask deps).
      Правила (ENTITY §18.2):
        R1. Forbid bare `tokio::spawn(` в любом .rs файле ВНЕ
            crates/runtime/src/. Whitelist: crates/runtime/src/supervisor.rs.
            Allow на test-файлах (tests/*.rs) если file-scope
            #[allow(clippy::disallowed_methods)] присутствует (already
            true for pool-validator integration test).
        R2. Forbid `lazy_static!` macro вне crates/common/src/registry/.
            (registry crate ещё не создан — пока правило просто никогда
            не сработает, но защита заложена.)
        R3. Forbid macros expanding to >50 LOC — пока эвристика:
            forbid macro_rules! blocks with >50 lines в одном definition.
            (Heuristic, not perfect — улучшим в month 2.)
      Все нарушения → stderr с file:line + цитата строки + ENTITY ref.
      Exit 1 если есть нарушения, exit 0 иначе.
      Skip patterns: target/, .git/, node_modules/, .sqlx/.
  M2. xtask/src/commands/mod.rs: pub mod magic_check;
  M3. xtask/src/main.rs: route Cmd::MagicCheck → commands::magic_check::run()
      (spine mini-edit — АВТОРИЗОВАН для M3).
  M4. Unit tests inside magic_check.rs:
        test_detects_bare_tokio_spawn — regex match on synthetic snippet
        test_skips_runtime_supervisor_path — same regex, runtime/supervisor.rs path → no flag
        test_skips_test_files_with_allow — file containing #![allow(clippy::disallowed_methods)] + tokio::spawn → no flag
  M5. Запустить `cargo run -p xtask -- magic-check` против текущего
      tree. Ожидание: 0 нарушений (всё чисто после P1 предыдущей AVTONOM).
      Если есть — log в SESSION_LOG, чини non-spine, иначе HARD STOP.

P2 · Final
  F1. SESSION_LOG.md (overwrite) — стандартная структура AVTONOM.
  F2. Один коммит: `feat(ax/g1-d1): xtask magic-check real impl`.
      Trailer `AI-Assisted: AX-ARCHITECT (Claude Opus 4.7)`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS (inherited)
═════════════════════════════════════════════════════════════════════

  • Версии библиотек: workspace Cargo.toml — read-only. Per-crate
    Cargo.toml допустимо (non-spine). xtask уже имеет regex + walkdir.
  • SQL: N/A сегодня.
  • Naming: snake_case modules, PascalCase types, test_<func>_<scenario>.
  • Git: sequential commits, bash для git, без rebase/amend.
  • Spine touch авторизован: только M3 (xtask/src/main.rs).

═════════════════════════════════════════════════════════════════════
HARD STOPS
═════════════════════════════════════════════════════════════════════

  1. Spine-touch без явной авторизации.
  2. cargo check > 5 итераций красное.
  3. magic-check на текущем tree обнаружил violations и они в spine —
     записать в SESSION_LOG как «known debt for W4 G5», НЕ чинить.
  4. Disk full / OOM.
  5. Internet beyond cargo registry.

═════════════════════════════════════════════════════════════════════
ЗАПРЕЩЕНО АБСОЛЮТНО
═════════════════════════════════════════════════════════════════════

  • git push, gh PR.
  • Изменение ENTITY.md, CLAUDE.md, workspace Cargo.toml, docker-compose.dev.yml,
    .env.example, clippy.toml, rustfmt.toml, rust-toolchain.toml, deny.toml.
  • Refactor вне SCOPE.
  • Удаление файлов.
  • cargo bench без --no-run, xtask bench-runner.
  • Новые workspace-deps.

═════════════════════════════════════════════════════════════════════
SESSION_LOG.md формат
═════════════════════════════════════════════════════════════════════

# SESSION_LOG — AVTONOM 2026-05-25 HH:MM (W1 D1)
## Outcome — one line per phase
## Plan (detailed status)
## AI-Defaults applied
## Skipped / Blocked
## Commits made (local, not pushed)
## Recommendations for human review
## Working tree at end of session
## Time budget
## CARRY-OVER for tomorrow (если есть → в daily/2026-05-26.md секцию CARRY-OVER)
