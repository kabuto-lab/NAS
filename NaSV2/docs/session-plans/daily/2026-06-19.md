AVTONOM: AX•CMS · W4 D5 · RETRO day + next-month bootstrap generation.
Дата: 2026-06-19 (пт). Senior Rust architect prompt. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
CONTEXT
═════════════════════════════════════════════════════════════════════

Last working day of 2026-05 (May 25 → Jun 19). Two-phase day:
  1. Retrospective summarising what happened across W1–W4
  2. Generate next-month bootstrap seeded by the retro

NO new feature work today — RETRO is RETROSPECTIVE only. Any
"would be nice to fix" findings go into RETRO §7 Next-month
proposals, NOT into a same-day commit.

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4) — month-end sweep

  Capture month-end snapshot:
  ```bash
  cargo check --workspace --all-targets 2>&1 \
    | tee docs/session-logs/avtonom-2026-06-19-cargo-check.log
  cargo clippy --workspace --all-targets -- -D warnings 2>&1 \
    | tee docs/session-logs/avtonom-2026-06-19-cargo-clippy.log
  cargo test --workspace --lib --no-fail-fast 2>&1 \
    | tee docs/session-logs/avtonom-2026-06-19-cargo-test.log
  ```

  All must be green for the month to count as successful per
  ROADMAP-2026-05 B6 exit criteria #1–#3.

  Also capture:
  ```bash
  cargo run -p xtask -- architecture-check
  cargo run -p xtask -- magic-check
  cargo run -p xtask -- capability-coverage
  cargo run -p xtask -- check-planning-refs --commit b67302e..HEAD
  ```

  (Range = MONTH_START commit b67302e through HEAD — covers every
  commit added this month.)

P1 · `docs/session-plans/RETRO-2026-06.md` (NEW)

  Structured exactly as planned in the bootstrap template — future
  AUDIT bots parse this shape.

  ```markdown
  # RETRO — AX•CMS / NaSV2 · 2026-05-25 → 2026-06-19

  ## 1 · Outcome by goal

  | Goal | Status | Notes |
  |---|:---:|---|
  | G1 magic-check + check-planning-refs (W1 D1–D3) | <✅/partial/skipped> | <one-liner> |
  | G2 nas2-common + nas2-domain (W1 D4 → W2 D4) | ... | ... |
  | G3 application::ports + TenantContext (W2 D5 → W3 D3) | ... | ... |
  | G4 first presentation handler + cap gate (W3 D4 → W4 D1) | ... | ... |
  | G5 capability-coverage + planning backfill + perf + ammonia (W4 D2–D4) | ... | ... |

  ## 2 · Velocity actual vs estimated

  - Estimated: 20 working days = ~160 productive hours
  - Actual: <count> AVTONOM sessions × <avg> min
  - Slack days used: <N>

  Compute by counting commits b67302e..HEAD grouped by `daily/2026-*.md`
  reference in commit message or by file-mtime on `SESSION_LOG.md`.

  ## 3 · Scope creep (added during month)

  List unplanned additions. Examples that might appear:
   - thread-local caps override in W4 D1 — not in original plan
   - OnceLock<Builder> for ammonia (vs per-call) in W4 D4

  ## 4 · Surprises (good and bad)

  - Good: `xtask architecture-check` caught domain dep creep on
    W1 D1 morning, prevented snowball
  - Bad: <whatever actually went sideways>

  ## 5 · Debt acquired (in tree as TODO/FIXME)

  ```bash
  grep -rn 'TODO\|FIXME' crates/ apps/ --include='*.rs'
  ```
  Paste the list. Each entry → next-month AUDIT candidate.

  ## 6 · Exit criteria from ROADMAP-2026-05 B6

  | # | Criterion | Status |
  |---:|---|:---:|
  | 1 | cargo check green | <✅/❌> |
  | 2 | cargo clippy -D warnings green | ... |
  | 3 | cargo test --lib ≥ 20 unit | ... |
  | 4 | xtask architecture-check ok | ... |
  | 5 | xtask magic-check ok + synthetic exit 1 | ... |
  | 6 | xtask capability-coverage ok + synthetic exit 1 | ... |
  | 7 | xtask check-planning-refs ok on clean tree | ... |
  | 8 | nas2-common/domain/application/tenant non-stub | ... |
  | 9 | ≥ 1 presentation handler 200/401/404 | ... |
  | 10 | docs/perf/baseline.json exists | ... |
  | 11 | planning trail orphans ≤ 2 | ... |
  | 12 | ≥ 18 daily AVTONOM commits + RETRO | ... |
  | 13 | working tree clean | ... |

  ## 7 · Next-month proposals (top 5; seed for next AUDIT)

  Numbered list — the next month's G1..G5 candidates. Suggested
  starters based on this month's deferred work:

  1. **Image pipeline phase 1**: wire `libvips` + pgmq worker
     (consumes G2 Media placeholder + RFC-004)
  2. **Real PgPostRepository + PgUserRepository**: implements W2 D5
     port traits against sqlx + RLS via set_config in transaction
  3. **JWT-derived caps extraction**: replace
     `extract_caps_for_today` stub with `jsonwebtoken`-based parse;
     close last "in production" gap on capability surface
  4. **PgTenantResolver with moka LRU**: replace `InMemoryTenantResolver`
     with DB-backed lookup; implement the `with_tenant` helper that
     ENTITY §3.5 demands (single-RTT set_config + query)
  5. **xtask check-planning-refs in CI**: wire after this month's
     backfill makes historical commits pass

  ## 8 · Metrics snapshot

  | Metric | Month-start (2026-05-25) | Month-end | Δ |
  |---|---|---|---:|
  | crate count (stub/partial/functional/production) | 9/4/3/0 | <new> | ... |
  | unit tests | 5 | <count> | ... |
  | integration tests | 5 | <count> | ... |
  | benches | 1 | <count> | ... |
  | xtask real gates | 3/11 | <count>/11 | ... |
  | planning trail orphans | 6 | <count> | ... |
  | commits in month | 0 | <count via `git log b67302e..HEAD --oneline | wc -l`> | ... |

  ## 9 · Operator action list

  1. Push `b67302e..HEAD` to origin/main when satisfied with review.
  2. Capture `docs/perf/baseline.json` on a quiet machine OR a CI
     runner; re-commit if today's bootstrap captured on a noisy host.
  3. Wire `check-planning-refs` into CI now that the trail is healed.
  4. Optionally: open next-month bootstrap.
  ```

P2 · Generate next-month bootstrap

  Copy `docs/session-plans/avtonom-month-bootstrap.md` to
  `docs/session-plans/avtonom-month-bootstrap-2026-07.md`. Two
  changes only:

  1. In the file's *outer* preamble (markdown above the AVTONOM
     fenced block), add:
     ```
     > Seeded by RETRO-2026-06.md (`docs/session-plans/RETRO-2026-06.md`).
     > Phase A PF1 should read that retro FIRST — its §7 (Next-month
     > proposals) is the input for the new G1..G5.
     ```
  2. In Phase A PF1 inside the fenced AVTONOM block, prepend:
     ```
     PF1.0. Read docs/session-plans/RETRO-2026-06.md (full); §7
            (Next-month proposals) is the SEED for B1 goals — do not
            re-derive from AUDIT alone, blend with RETRO.
     ```

  All other content stays identical — the bootstrap is a stable
  template by design.

P3 · Final
  Two sequential commits (not collapsed — separation aids RETRO
  archive auditing):

  Commit 1:
  ```
  docs(ax/retro,VAL-001): RETRO-2026-06 month-end report

  Outcome by goal, velocity actual vs estimated, scope creep,
  surprises, debt acquired (grep TODO/FIXME), exit criteria
  evaluation, next-month proposals (top 5), metrics snapshot,
  operator action list.

  Concludes 2026-05-25 → 2026-06-19 month.

  AI-Assisted: AX-ARCHITECT (Claude Opus 4.7)
  ```

  Commit 2:
  ```
  chore(ax/bootstrap,PLAN-G1): next-month bootstrap (2026-07) generated

  docs/session-plans/avtonom-month-bootstrap-2026-07.md is structural
  twin of current bootstrap with two surgical edits:
   - preamble notes RETRO-2026-06 as seed
   - Phase A adds PF1.0 — read RETRO §7 first

  Operator action: on 2026-06-22 (next Mon), paste this file as
  opening message of a new Claude Code session. Bootstrap will
  produce AUDIT-2026-06-22 / ROADMAP-2026-07 / WEEK-* / daily/* /
  HOW-TO-RUN refresh.

  AI-Assisted: AX-ARCHITECT (Claude Opus 4.7)
  ```

═════════════════════════════════════════════════════════════════════
COMMON PITFALLS
═════════════════════════════════════════════════════════════════════

  1. **Don't write new feature code today.** Any temptation = next-
     month proposal.
  2. **Don't edit prior `SESSION_LOG.md` versions** (those are commit-
     archived per-day already). Today's RETRO is a NEW file at a
     different path.
  3. **Metrics counts.** `cargo test --workspace --lib --no-fail-fast`
     final tail shows the test totals. Use:
     ```bash
     cargo test --workspace --lib --no-fail-fast 2>&1 | \
       grep -E '^test result:' | \
       awk '{s+=$4} END {print s}'
     ```
     to sum across crates.
  4. **`git log b67302e..HEAD --oneline | wc -l`** — bootstrap-session
     commit count. Should be roughly 25-30 (5 bootstrap + 6 month-
     bootstrap + 18-20 daily + 1-2 RETRO).
  5. **Bootstrap-2026-07 is for the human to RUN, not to execute
     today.** Do NOT trigger it as a side effect of writing it.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Two commits (RETRO + bootstrap), not one — separation aids
    historical analysis.
  • RETRO §7 has 5 proposals (matches G1..G5 cardinality of a
    month's roadmap).
  • Bootstrap-2026-07 is structurally identical — DO NOT redesign
    today.
  • All exit criteria evaluated explicitly (✅/❌/partial) — no
    "mostly green" hand-waving.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG (standard)
═════════════════════════════════════════════════════════════════════

Standard. Final SESSION_LOG.md overwrite at NaSV2 root with the
month-end summary; W4 D5's `## CARRY-OVER for tomorrow` becomes
`## Next month begins 2026-06-22 — operator pastes
avtonom-month-bootstrap-2026-07.md`.
