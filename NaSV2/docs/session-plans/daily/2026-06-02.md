AVTONOM: AX•CMS · W2 D2 · G2.P4 — nas2-domain::post.
Дата: 2026-06-02 (вт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-domain::post` aggregate
  P1. crates/domain/src/post.rs:
        pub struct Post {
            pub id: PostId,        // newtype в crates/common/src/ids.rs
            pub site_id: SiteId,
            pub tenant_id: TenantId,
            pub slug: PostSlug,
            pub title: String,
            pub status: PostStatus,
            pub published_at: Option<chrono::DateTime<chrono::Utc>>,
            pub blocks: Vec<crate::block::Block>,  // Block enum — D4
        }
        pub enum PostStatus { Draft, Scheduled, Published, Archived }
        pub struct PostSlug(String);  // те же invariants как SiteSlug
        pub struct CustomPostType(String);  // placeholder для extensions
  P2. crates/common/src/ids.rs: добавить PostId(Uuid) + new_v4().
  P3. crates/domain/src/lib.rs: pub mod post; re-exports.
  P4. Unit tests (≥ 8):
        test_post_status_transitions_draft_to_published / to_archived
        test_post_status_transitions_forbidden (Archived → Published reject)
        test_post_slug_validation (same battery as SiteSlug)
        test_post_serde_roundtrip
        test_post_published_requires_published_at  // invariant
        test_custom_post_type_serde

P2 · `nas2-domain::block` PLACEHOLDER
  B1. crates/domain/src/block.rs:
        // Заглушка до W2 D4 (когда полный Block enum приедет).
        // Сейчас один вариант, чтоб Post::blocks компилировался.
        #[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
        pub enum Block {
            Placeholder,  // удалим в D4
        }
      Один test: test_block_placeholder_roundtrip.

P3 · architecture-check + clippy
  A1, A2 — стандартно.

P4 · Final
  F1. SESSION_LOG.md.
  F2. Коммит: `feat(ax/g2-w2d2,PLAN-G2): nas2-domain::post + PostStatus + PostSlug + Block placeholder`.
      Trailer AI-Assisted.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • PostStatus FSM: Draft→Scheduled, Draft→Published, Scheduled→Published,
    Published→Archived, Archived→(terminal). Запрещено возвращение из
    Archived в Published.
  • CustomPostType — placeholder, без validation (плагины приходят месяц 2+).
  • PostId добавляется в nas2-common (не в domain) — Post composes из IDs.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
