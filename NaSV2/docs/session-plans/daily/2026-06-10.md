AVTONOM: AX•CMS · W3 D3 · G3.P4 — capability hash + MediaRepository port stub + G3 close-out.
Дата: 2026-06-10 (ср). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · Promote `CapabilitySet::hash_xx` → стабильный `cache_key_hash`
  H1. crates/domain/src/capability.rs:
        // Заменить hash_xx на cache_key_hash(seed: u64) -> u64
        // Используя std::hash::{DefaultHasher, Hasher} с фиксированным seed
        // через std::hash::BuildHasherDefault — детерминированный per-run,
        // но per-run hash МОЖЕТ отличаться между Rust versions. Для cache
        // invalidation между процессами это OK — общий cache invalidates
        // на restart.
        // Документировать non-cryptographic + non-cross-process intent.
  H2. Unit tests — cache_key_hash_is_deterministic_within_process,
        cache_key_hash_differs_for_different_capability_sets.

P2 · `nas2-application::ports::media_repository` STUB
  M1. crates/application/src/ports/media_repository.rs:
        // Stub — будет реализован при первой media-upload endpoint
        // (month 2+). Сейчас только trait + 1 placeholder method.
        #[async_trait::async_trait]
        pub trait MediaRepository: Send + Sync {
            // First real method: upload(MediaId, bytes) — отложено.
            // На данный момент только find_by_id с #[allow(unused)]
            async fn find_by_id(&self, tenant: TenantId, id: MediaId)
                -> Result<Option<Media>, AppError>;
        }
        // Media + MediaId — placeholder types в этом же файле (move в
        // domain когда первая реализация придёт)
        pub struct Media { pub id: MediaId, pub url: String }
        pub struct MediaId(uuid::Uuid);
  M2. mod.rs re-export.
  M3. Unit test: object-safety check.

P3 · G3 close-out polish
  G1. grep TODO/FIXME в crates/application и crates/tenant — fix или
      задокументировать в SESSION_LOG как «открытые для G4 / month 2».
  G2. cargo doc --workspace --no-deps 2>&1 | tail -50 — поймать
      doc-warnings (broken intra-doc links etc.). Если есть — fix.

P4 · Final
  Коммит: `feat(ax/g3-w3d3,PLAN-G3): CapabilitySet::cache_key_hash + MediaRepository port stub`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • cache_key_hash — non-cryptographic, per-process stable.
    Cross-process determinism — отдельная задача (xxh3_64 крейт
    xxhash-rust добавляется в month 2 если потребуется).
  • MediaRepository — minimal stub; реальные методы (upload, derive_variant,
    delete) приходят с image-pipeline (month 2+).

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
