AVTONOM: AX•CMS · W2 D4 · G2.P6 — nas2-domain::block enum finalization.
Дата: 2026-06-04 (чт). Дефолты ниже — финальные. Вопросов нет. Не пушить.

## CARRY-OVER from yesterday: (none)

═════════════════════════════════════════════════════════════════════
SCOPE
═════════════════════════════════════════════════════════════════════

P0 · Verification gate (V1..V4)

P1 · `nas2-domain::block` — replace placeholder
  B1. crates/domain/src/block.rs (overwrite):
        #[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
        #[serde(tag = "type", rename_all = "snake_case")]
        pub enum Block {
            Heading { level: HeadingLevel, text: String },
            Paragraph { html: String },              // pre-sanitized HTML
            Image { src: String, alt: String, caption: Option<String> },
            CodeBlock { lang: Option<String>, code: String },
        }
        pub enum HeadingLevel { H1, H2, H3, H4, H5, H6 }
        impl Block { pub fn block_type(&self) -> &'static str { ... } }
  B2. Удалить вариант Block::Placeholder. Все consumers в Post —
      проверить cargo check.
  B3. Unit tests (≥ 8):
        test_heading_serde / test_paragraph_serde / test_image_serde /
        test_codeblock_serde_with_lang / test_codeblock_serde_no_lang /
        test_block_type_names_stable / test_block_unknown_type_rejected /
        test_post_with_mixed_blocks_serde

P2 · Domain аудит — проверить полноту
  D1. cargo xtask architecture-check → ok
  D2. grep '\btodo!\|unimplemented!' crates/domain/src/ → expect 0
  D3. Сосчитать unit-тесты в domain: должно быть ≥ 25 на этом этапе.

P3 · Final
  F1. SESSION_LOG.md.
  F2. Коммит: `feat(ax/g2-w2d4,PLAN-G2): nas2-domain::block enum (Heading/Paragraph/Image/CodeBlock)`.

═════════════════════════════════════════════════════════════════════
PRE-RESOLVED DEFAULTS
═════════════════════════════════════════════════════════════════════

  • Paragraph::html — это PRE-SANITIZED HTML (sanitization будет
    on-write в W4 D4). НЕ хранить raw user input.
  • HeadingLevel — типизированный enum, НЕ u8. Запрещает H7+.
  • Block::Image — `src: String` пока — заменим на MediaId когда
    crates/domain/src/media.rs появится (deferred to month 2).
  • Serde tagged repr — `{"type": "heading", "level": "h2", ...}`.

═════════════════════════════════════════════════════════════════════
HARD STOPS / ЗАПРЕЩЕНО / SESSION_LOG формат
═════════════════════════════════════════════════════════════════════

(стандарт)
