# WEEK-04 · Jun 15–19, 2026

> Theme: **Handler completion + capability gate + planning backfill + RETRO**
> Goals covered: finish **G4** + all of **G5**
> Generated by AVTONOM month-bootstrap from `ROADMAP-2026-05.md`.

## Daily slots

| Date | Day | Daily prompt file | Scope summary | Est h |
|---|---|---|---|---:|
| 2026-06-15 | Mon | `daily/2026-06-15.md` | G4 finish · error mapping (404 vs 401 vs 400 vs 500), structured JSON error body, capability-gate helper extracted from W3 D5, ≥ 3 axum oneshot tests covering all status codes | 4 |
| 2026-06-16 | Tue | `daily/2026-06-16.md` | G5.P1 · xtask `capability-coverage` real impl — regex scan of `crates/presentation/src/**/*.rs` for handler-shaped fns; require either `caps.require(...)` call or `#[no_capability_required]` doc-attr; synthetic violation test | 4 |
| 2026-06-17 | Wed | `daily/2026-06-17.md` | G5.P2 · planning trail backfill — RFC-001 (cms-pages-publish), RFC-003 (task-supervisor), RFC-004 (pgmq-queue), ADR-001 (cms-pages-architecture), VAL-001 (cms-pages-tests) | 4 |
| 2026-06-18 | Thu | `daily/2026-06-18.md` | G5.P3 · first perf baseline (`docs/perf/baseline.json` for `pool_mode_from_str`); ammonia sanitize-on-write helper in `nas2-common::sanitize` (single-call wrapper); SEC-002 (capability surface) + SEC-003 (multi-tenant isolation invariants) docs | 4 |
| 2026-06-19 | Fri | `daily/2026-06-19.md` | RETRO day · `RETRO-2026-06.md` + `docs/session-plans/avtonom-month-bootstrap-2026-06.md` (next-month bootstrap, regenerated from this retro) | 3 |

## Dependencies satisfied entering this week

- `GET /api/v1/pages/:slug` handler returns valid responses for happy path
  (from W3 D5)
- `nas2-tenant::TenantContext` middleware mounted and tested
- `CapabilitySet::hash()` available for cache-key composition
- xtask `architecture-check`, `magic-check`, `check-planning-refs` real

## Definition of done (W4)

- [ ] `/api/v1/pages/:slug` returns 200/401/404/400 correctly under
  axum `oneshot()` (3+ tests)
- [ ] `cargo xtask capability-coverage` rejects a handler with a
  synthetic missing `caps.require()`; passes on clean tree
- [ ] All 6 planning-trail orphans from AUDIT A3 have partner docs
  (orphan count ≤ 2 — image-pipeline + search-engine, which remain
  legitimately deferred)
- [ ] `docs/perf/baseline.json` has at least the `pool_mode_from_str`
  series with criterion-extracted ns/iter numbers
- [ ] `crates/common::sanitize::clean_html(input: &str) -> String`
  wraps ammonia with the documented allow-list; ≥ 4 unit tests covering
  script tag stripping, attribute filtering, URL scheme allow-list
- [ ] `RETRO-2026-06.md` exists with the 9 standard sections (outcome /
  velocity actual vs estimated / scope creep / goals achieved /
  surprises / debt acquired / next-month proposals / metrics snapshot
  / operator action list)
- [ ] `docs/session-plans/avtonom-month-bootstrap-2026-06.md` exists
  and is structurally identical to the current `avtonom-month-bootstrap.md`
  with month-specific MONTH_START/MONTH_END updated and AUDIT findings
  carried into the next month's goal set as input
- [ ] 5 daily commits (W4 D5 is two: RETRO commit + next-bootstrap commit)
- [ ] 0 `git push`

## Carry-over policy

By W4 D4 any unfinished work moves to the **"deferred to month 2"**
section in `RETRO-2026-06.md` rather than cascading to D5. W4 D5 is
sacred — only RETRO + next-bootstrap.

If W4 D4 itself carries over from D3, the operator may collapse D3 + D4
into a single AVTONOM session manually (and the daily prompts both note
this is acceptable).

## Risks specific to W4

| Risk | Mitigation |
|---|---|
| `capability-coverage` finds handlers missing the gate → ENTITY §14 violation in the very handler just shipped | day's first action: audit existing handler, add `caps.require()` if missing; treat the regex impl as the proof of correctness |
| RFC backfill drifts into "rewrite history" temptation | docs are NEW (written today); never rewrite git history, never edit committed commit messages |
| Perf baseline captured on noisy dev hardware skews future regression detection | document the capture conditions in `baseline.json` metadata; flag for re-capture in next-month bootstrap |
| `RETRO-2026-06` includes things to "fix in this month" — scope drift | RETRO is RETROSPECTIVE only; all "to fix" items become candidates for next-month AUDIT, NOT this month |
| Next-month bootstrap pulls in template before the retro is complete | hard order: complete RETRO commit, THEN generate next bootstrap |
