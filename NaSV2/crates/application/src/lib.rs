//! `nas2-application` · L2 use cases + port traits · ENTITY §2, §8
//!
//! Содержит: port traits (`PostRepository`, `UserRepository`, `MediaRepository`,
//! ...), use cases (commands + queries), capability check helpers, hook registry
//! interface.
//!
//! Implementation pending. См. ENTITY §8 capability, §9 plugin/extension API.

#![forbid(unsafe_code)]

pub mod ports;
// pub mod commands;
// pub mod queries;
// pub mod services;
// pub mod hooks;
// pub mod capability;
