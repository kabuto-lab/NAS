[package]
name        = "nas2-pool-validator"
version.workspace     = true
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
publish.workspace     = true
authors.workspace     = true

# ENTITY §3.4.1 — Pool-mode contract: runtime gate that refuses to boot
# the server unless `SHOW pool_mode` returns `transaction`.

[lints]
workspace = true

[dependencies]
sqlx       = { workspace = true }
thiserror  = { workspace = true }
tracing    = { workspace = true }
tokio      = { workspace = true }

[dev-dependencies]
# Integration tests (#[ignore = "needs Docker"]) — VAL-002 pool-mode contract.
testcontainers         = { workspace = true }
testcontainers-modules = { workspace = true }
uuid                   = { workspace = true }
